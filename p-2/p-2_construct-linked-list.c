// Original Problem: Based on the given struct expressing a node of a linked list, construct
// a linked list representing a given integer in reversed order.

struct ListNode {
  int val;
  struct ListNode *next;
};
