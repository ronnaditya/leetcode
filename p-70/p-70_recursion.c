#include<stdio.h>
#include<stdlib.h>

int climbStairs(int n) {
  if (n <= 2) {
    return n;
  } else {
    int ways = 0;
    int i = 0;
    while (i <= n) {
      ways += 2
    }
  }
}

void main() {
  printf("%d", climbStairs(45));
}
